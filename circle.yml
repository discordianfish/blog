machine:
  nodejs: 4.2.6

dependencies:
  pre:
    - npm install
    - cat npm-debug.log || true

test:
  override:
    - "true"

deployment:
  production:
    branch: master
    commands:
      - npm run deploy
