general:
  branches:
    ignore:
      - gh-pages

machine:
  node:
    version: 6.1.0

dependencies:
  pre:
    - npm install
    - cat npm-debug.log || true

test:
  override:
    - "true"

deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "circle@5pi.de"
      - git config --global user.name "Johannes 'fish' Ziemke"
      - GIT_SSH_COMMAND="ssh -i $HOME/.ssh/id_358dbf85a58fd5d1b2e4131e3b14cf1e" npm run deploy
